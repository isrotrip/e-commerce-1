# e-commerce

Route | HTTP | Header(s) | Body | Response | Description
------|------|-----------|------|----------|------------
/users/login|POST||email:String(**Required**)<br>password:String(**Required**)<br>loginVia:String(**Required**)|**Error Response:**<br>status code 400 <br> return Object with keys: <br> - message:String <br>**Success Response:**<br>status code 200 <br> return Object with keys: <br> - token:String <br> - userLogin:Object <br> - message:String | **Error** <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> token is decoding from JWT <br> userLogin will give information that needed for client side
/users/register|POST||name:String(**Required**)<br>email:String(**Required**)<br>password:String(**Required**)<br>beAdmin:String(**required**) for become admin| **Error Response:**<br>status code 400<br> return Object with keys: <br> - message:String <br>**Success Response:**<br> status code 200 <br> return Object with keys <br> message:String <br> userInfo:Object | Register page for admin and customer should be differ in client side <br> **Error** <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> userInfo will give you data that you input from registration
/products|POST|token:String(**Required**)|name:String(**Required**)<br>amount:Number(**Required**) <br> price:Number(**Required**) <br> images:File(**Required**) <br> created_at:Date(**Required**) <br> updated_at:Date(**Required**) | **Error Response:**<br> status code 400 <br> return Object with keys: <br> message:String <br> **Success Response:**<br> Status code 201 <br> return Object with keys: <br> message:String <br> details:Object | **Error** <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> details will give you data of a product you create
/products|GET||| **Error Response:** <br> Internal server error <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> products:Array | **Error** <br> only happen if database or server are down <br> **Success** <br> message to make UI better in client side <br> products are array of the products is on the database
/products/:id|GET||| **Error Response:** <br> Internal server error <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> products:Object | **Error** <br> only happen if database or server are down <br> **Success** <br> message to make UI better in client side <br> product is object of the products that you want to find
/products/:id|PUT|token:String(**Required**) | itemId:String(**Required**) name:String(**Required**) <br> amount:Number(**Required**) <br> price:Number(**Required**) <br> picture:File(**Optional**) <br> created_at:Date(**Required**) <br> expired_date(**Required**) | **Error Response:**<br> status code 401 <br> return Object with keys: <br> - message:String <br> status code 404 <br> return Object with keys: <br> - message:String <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**<br> return Object with keys: <br> - message:String <br> - product:Object | **Error** <br> Status code 401 if the reason about authorization <br> status code 404 if the reason about wrong id inserted or data doesn't available in database <br> status code 400 if the wrong input from the user <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> product is product that successfully updated 
/products/:id|DELETE|token:String(**Required**)||**Error Response:**<br> status code 401 <br> return Object with keys:<br> -message:String <br> status code 400 <br> return Object with keys:<br> -message:String <br> status code 404 <br> return Object with keys:<br> - message:String <br> **Success Response:**<br> status code 200 <br> return Object with keys: <br> - message:String <br> - product:Object | **Error** <br> Status code 401 if the reason about authorization <br> status code 404 if the reason about wrong id inserted or data doesn't available in database <br> status code 400 if the wrong input from the user <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> product is product that successfully deleted 
/carts|POST|token:String(**Required**)| amount:Number(**Required**) <br> productId:String(**Required**) | **Error Response:**<br> status code 400 <br> return Object with keys: <br> message:String <br> **Success Response:**<br> Status code 201 <br> return Object with keys: <br> message:String <br> cart:Object | **Error** <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> cart will give you data of a cart you create
/carts|GET|token:String(**Required**)|| **Error Response:** <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> carts:Array | **Error** <br> when user provide wrong or invalid token <br> **Success** <br> message to make UI better in client side <br> carts are array of the carts is on the database
/carts/:id|PUT| token:String(**Required**) | amount:Number(**Required**) | **Error Response:**<br> status code 401 <br> return Object with keys: <br> - message:String <br> status code 404 <br> return Object with keys: <br> - message:String <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**<br> return Object with keys: <br> - message:String <br> - cart:Object | **Error** <br> Status code 401 if the reason about authorization <br> status code 404 if the reason about wrong id inserted or data doesn't available in database <br> status code 400 if the wrong input from the user <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> cart is cart that successfully updated 
/carts/:id|DELETE|token:String(**Required**)||**Error Response:**<br> status code 401 <br> return Object with keys:<br> -message:String <br> status code 400 <br> return Object with keys:<br> -message:String <br> status code 404 <br> return Object with keys:<br> - message:String <br> **Success Response:**<br> status code 200 <br> return Object with keys: <br> - message:String <br> - cart:Object | **Error** <br> Status code 401 if the reason about authorization <br> status code 404 if the reason about wrong id inserted or data doesn't available in database <br> status code 400 if the wrong input from the user <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> cart is cart that successfully deleted
/transactions/rajaOngkir|GET|token:String(**Required**)|| **Error Response:** <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> dataOngkir:Object | **Error** <br> when user provide wrong or invalid token <br> **Success** <br> message to make UI better in client side <br> dataOngkir is object of the send information data
/transactions/rajaOngkir|GET|token:String(**Required**)|| **Error Response:** <br> status code 400 <br> return Object with keys: <br> - message:String <br> status code 401 <br> return Object with keys: <br> - message:String  <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> dataOngkir:Object | **Error** <br> status code 400 when user provide wrong or invalid token <br> status code 401 if unauthorized user try to post <br> **Success** <br> message to make UI better in client side <br> dataOngkir is object of the send information data
/transactions/rajaOngkir|POST|token:String(**Required**)|origin:String(**Required**) <br> destination:String(**Required**) <br> courier:String(**Required**)| **Error Response:** <br> status code 400 <br> return Object with keys: <br> - message:String <br> status code 401 <br> return Object with keys: <br> - message:String  <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> info:Object | **Error** <br> status code 400 when user provide wrong or invalid token <br> status code 401 if unauthorized user try to post <br> **Success** <br> message to make UI better in client side <br> info is object of the send information data
/transactions|POST|token:String(**Required**)| cartId:String(**Required**) <br> origin:String(**Required**) <br> destination:String(**Required**) <br> courier:String(**Required**) <br> courier_fullname:String(**Required**) <br> service:String(**Required**) <br> price:Number(**Required**) etd:String(**Required**) | **Error Response:**<br> status code 400 <br> return Object with keys: <br> message:String <br> **Success Response:**<br> Status code 201 <br> return Object with keys: <br> message:String <br> transaction:Object | **Error** <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> transaction will give you data of a transaction you create
/transactions|GET|token:String(**Required**)|| **Error Response:** <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**:<br> status code 200 <br> return Object with keys: <br> message:String <br> transactions:Array | **Error** <br> when user provide wrong or invalid token <br> **Success** <br> message to make UI better in client side <br> transactions are array of the transactions is on the database
/transactions/:id|PUT| token:String(**Required**) | status:String(**Required**) | **Error Response:**<br> status code 401 <br> return Object with keys: <br> - message:String <br> status code 404 <br> return Object with keys: <br> - message:String <br> status code 400 <br> return Object with keys: <br> - message:String <br> **Success Response:**<br> return Object with keys: <br> - message:String <br> -transaction:Object | **Error** <br> Status code 401 if the reason about authorization <br> status code 404 if the reason about wrong id inserted or data doesn't available in database <br> status code 400 if the wrong input from the user <br> message inform error reason <br> **Success** <br> message to make UI better in client side <br> transaction is transaction that successfully updated 
|


### Usage
command |
------- |
$ npm install |
$ npm run start from server dist |
$ npm run serve |

Access the Server via http://localhost:3000/
<br>
Access the Client via http://localhost:8080/